{
  "name": "go-npm-next",
  "version": "1.0.2",
  "description": "Distribute and install Go binaries via NPM",
  "main": "./src/index.js",
  "bin": {
    "go-npm": "./bin/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/esbuild src/index.js --bundle --outdir=bin --target=es2015 --platform=node",
    "prepublish": "npm run build"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "mkdirp": "^0.5.1",
    "request": "^2.81.0",
    "tar": "^6.1.13"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hookdeck/go-npm.git"
  },
  "homepage": "https://github.com/hookdeck/go-npm",
  "devDependencies": {
    "esbuild": "^0.17.11"
  }
}
